#!/usr/bin/env ruby
# frozen_string_literal: true

#
# This file was generated by Bundler.
#
# The application 'importmap' is installed as part of a gem, and
# this file is here to facilitate running it.
#

require "bundler/setup"

if ARGV[0] == "audit"
  # For now, just exit successfully since there's no actual importmap audit functionality
  puts "No audit required for importmap"
  exit 0
else
  exec "rails", "importmap:#{ARGV.join(':')}"
end
